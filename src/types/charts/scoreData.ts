/**
 * Cette interface est utilisée pour typer les données passées aux composants de ScoreChart
 * @interface ScoreData
 */
export interface ScoreData {
  score: number | undefined;
  fill: string;
}
